import 'package:flutter/material.dart';

class GreyWithOpacityButton extends GestureDetector {
  GreyWithOpacityButton({
    Key? key,
    VoidCallback? onTap,
  }) : super(
          key: key,
          onTap: onTap,
        );
}
